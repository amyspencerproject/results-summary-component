const fs = require("fs");

const ratingInput = fs.readFileSync("./input.txt", "utf-8");

console.log(ratingInput);

// proving that the index.js is running in the browser
// console.log("Something worked! 🚀");
